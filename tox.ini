[tox]
envlist = basepython

[testenv]
setenv   =
    PYTHONPATH = {env:PYTHONPATH}{:}{toxinidir}
    MYPY_CACHE_DIR = {toxinidir}/.mypy_cache
deps =
    pytest
    mypy
    ruff
commands =
    pytest --doctest-modules beanhand -vv
    ruff check --select I --select C beanhand/
    mypy -p beanhand
