[tox]
envlist = basepython

[testenv]
setenv   =
    PYTHONPATH = {env:PYTHONPATH}{:}{toxinidir}
deps =
    pytest
    mypy
    ruff
commands =
    pytest
    mypy -p beancount_ai
    ruff check beancount_ai/
